# ✈️ Responsive Signup and Signin Portal with JS Validations

### Project Profile
- **Developer Name:** Leelavathy S
- **Course Module:** Full Stack Development (V8) - Module End Assignment
- **Prescribed Structure:** Adheres perfectly to the `/css`, `/js`, and `/Images` directory layout requirements.

### Core Architecture Features
1. **Semantic HTML5 Layout:** Structured using `<main>`, `<header>`, `<section>`, and `<footer>` elements.
2. **Unified Branding UI:** Custom card layouts styled uniformly with Bootstrap 5 across registration and login modules.
3. **Advanced Responsive Breakpoints:** Custom CSS media queries handling rendering behaviors across Mobile (≤768px), Tablet (769–1024px), and Desktop (>1024px) screens.
4. **JavaScript Authentication & Persistence Subsystem:** Client-side registration data maps securely to `localStorage` buffers, supporting runtime input validations and credential filtering.
5. **Show/Hide Toggle & Inline Validation Indicators:** Interactive inputs giving real-time feedback with dynamic validation color indicators.
